# macOS bootstrap: `brew bundle` from repo root (after Homebrew is installed).
# Container runtime for `pnpm exec supabase start` — do not use Docker Desktop (see project plan).

brew "asdf"
cask "rancher"
